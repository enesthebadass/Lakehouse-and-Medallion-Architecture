apiVersion: 1

prune: true

datasources:
  - name: Prometheus
    uid: lakehouse-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      prometheusType: Prometheus
      prometheusVersion: 3.12.0
      timeInterval: 15s
