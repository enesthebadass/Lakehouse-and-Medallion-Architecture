route:
  receiver: local-null
  group_by: [alertname, severity, service]
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-null

# Local PoC intentionally has no external notification credentials. Production must
# replace this receiver with the bank's approved incident-management integration.
